import { getPixelRatio } from '../../src/common/pixelRatio'

describe('getPixelRatio', () => {
  test('stub:browser-only - test manually in browser', () => {
    expect(true).toBe(true)
    void getPixelRatio()
  })
})
